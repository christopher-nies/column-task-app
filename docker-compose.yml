services:
  app:
    image: ghcr.io/christopher-nies/column-task-app:latest
    ports:
      - "3000:3000"
    volumes:
      - task_data:/app/data
    environment:
      - PORT=3000
      - DATA_DIR=/app/data
    restart: unless-stopped

volumes:
  task_data:
